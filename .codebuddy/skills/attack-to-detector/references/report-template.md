# 攻击分析报告标准模板

> 复制此模板并填充具体内容。所有占位符用 `{...}` 标记。

---

# {项目名} 攻击交易分析 — 通用检测特征提炼

> **交易哈希**: `{tx_hash}`
> **区块**: {block_number} | **时间**: {timestamp} | **链**: {chain_name}
> **分析日期**: {analysis_date}
> **分析方法**: 从单笔攻击交易中提炼**不依赖函数签名**的通用攻击特征，驱动检测器扩展

---

## 一、攻击概述

攻击者通过 {攻击手法概述}，{核心操作描述}，最终净赚 {利润金额}。

**攻击本质**: {一句话概括攻击本质，如"闪电贷 + 流动性操纵 + 代币合约漏洞的复合攻击"}

**攻击类型分类**: {攻击类型名称（英文分类名）}

---

## 二、资金流向

```
攻击者 EOA (0x{addr}...)
    │ {tx.value} {native_token}
    ▼
攻击合约 (0x{addr}...)  [Receiver, 函数 {selector}]
    │
    ├─[1] {操作1描述}: {金额} {代币}
    │
    ├─[2] {操作2描述}: {金额} {代币}
    │   ├─ {子操作}
    │   ├─ {子操作}
    │   └─ {子操作}
    │
    ├─[N] {利润提取}: {金额} → 攻击者 EOA
```

**投入产出**:

| 指标 | 值 |
|------|-----|
| 投入 (tx.value) | {投入} |
| 闪电贷借入 | {借入金额}（如有） |
| 产出 | {产出金额} |
| **净利润** | **{净利润}** |
| **ROI** | **{ROI}** |

---

## 三、通用攻击特征（不依赖函数签名）

以下特征完全从**资金流、事件日志、地址关系**中提取，不依赖任何特定函数签名，适用于跨协议检测。

---

### 特征 A：{特征名称}

**定义**: {特征的通用定义}

**本交易表现**:

| 指标 | 值 |
|------|-----|
| {指标1} | {值1} |
| {指标2} | {值2} |

**关键异常**:

- {异常1}
- {异常2}

**检测方法**:

```
1. {检测步骤1}
2. {检测步骤2}
3. {判定条件} → {信号名称}
```

**通用性说明**: {该特征在不同协议/链上的适用性说明}

---

### 特征 B：{特征名称}

（同上格式，每条特征独立一个小节）

---

## 四、特征组合与评分矩阵

| 特征组合 | 评分 | 含义 |
|----------|------|------|
| A 单独 | {score} | {含义} |
| A + B | {score} | {含义} |
| A + B + C | {score} | {含义} |
| 全部命中 | {score} | {含义} |

---

## 五、与现有检测器的覆盖度分析

### 现有检测器覆盖情况

| 检测器 | 特征覆盖 | 缺口 |
|--------|----------|------|
| `{DetectorA}` | 部分覆盖 X | **不检测 Y** |
| `{DetectorB}` | 不覆盖 | — |

### 核心缺口

1. **{缺口1描述}**: {详细说明}
2. **{缺口2描述}**: {详细说明}

---

## 六、检测器扩展建议

### 6.1 增强 `{DetectorName}`（P0 — {理由}）

**新增能力**:

| 增强项 | 描述 | 实现方式 |
|--------|------|----------|
| {增强1} | {描述} | {实现方式} |

**评分增强**:

```
当前: {当前评分逻辑}
增强: {增强后评分逻辑}
```

**关键实现要点**:

- {要点1}
- {要点2}

---

### 6.2 新增 `{DetectorName}`（P1 — {理由}）

**定位**: {检测器定位描述}

**核心检测维度**:

| 维度 | 检测方法 | 通用事件 |
|------|----------|----------|
| {维度1} | {方法} | {事件} |

**评分逻辑**:

```
基础分 = {公式}
加分 = {公式}
```

---

### 6.3 检测器优先级排序

| 优先级 | 检测器 | 理由 |
|--------|--------|------|
| P0 | {检测器} | {理由} |
| P1 | {检测器} | {理由} |
| P2 | {检测器} | {理由} |

---

## 七、本交易涉及的关键地址

| 地址 | 标签 | 角色 |
|------|------|------|
| `0x{addr}` | {标签} | {角色} |

---

## 八、关键事件日志索引

| Log 索引 | 事件 | 合约 | 关键数据 |
|----------|------|------|----------|
| {idx} | {event} | {contract} | {data} |

---

## 九、总结

本笔攻击暴露了一个**{核心发现}**。

### 攻击特征与现有检测器的关系

```
攻击特征                    现有检测器覆盖
─────────────────────────────────────────────
{特征1}                    ❌/⚠️/✅ {说明}
{特征2}                    ❌/⚠️/✅ {说明}
```

### 核心发现

1. **{发现1}**
2. **{发现2}**

### 最小可行扩展路径

1. **第一步**: {步骤1}
2. **第二步**: {步骤2}
3. **第三步**: {步骤3}
